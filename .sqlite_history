ALTER TABLE spots ADD COLUMN occupied_by TEXT;
ALTER TABLE spots ADD COLUMN occupied_until TIMESTAMP;
.exit
database.db
-- USERS
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
-- SPOTS
CREATE TABLE spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
-- RATINGS (MET COMMENT!)
CREATE TABLE ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
-- IMAGES
CREATE TABLE images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
.exit
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
CREATE TABLE spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
CREATE TABLE ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
CREATE TABLE images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
.exit
.table
.exit
CREATE TABLE IF NOT EXISTS favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
SELECT * FROM favorites;
SELECT * FROM;
sqlite3 database.db
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
CREATE TABLE IF NOT EXISTS spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
CREATE TABLE IF NOT EXISTS ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
CREATE TABLE IF NOT EXISTS images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
CREATE TABLE IF NOT EXISTS favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
CREATE TABLE IF NOT EXISTS spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
CREATE TABLE IF NOT EXISTS ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
CREATE TABLE IF NOT EXISTS images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
CREATE TABLE IF NOT EXISTS favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
.tables
.table
sqlite3 database.db
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
CREATE TABLE spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
CREATE TABLE ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
CREATE TABLE images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
CREATE TABLE favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT,
    password TEXT
);
CREATE TABLE spots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
CREATE TABLE ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    username TEXT,
    rating INTEGER,
    comment TEXT
);
CREATE TABLE images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    spot_id INTEGER,
    filename TEXT
);
CREATE TABLE favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
.table
.exit
CREATE TABLE IF NOT EXISTS favorites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT,
    spot_id INTEGER
);
.table
CREATE UNIQUE INDEX IF NOT EXISTS idx_fav_unique
ON favorites(username, spot_id);
UPDATE spots SET occupied_by=NULL WHERE occupied_by='';
UPDATE spots SET occupied_by=NULL WHERE occupied_by='None';
UPDATE spots SET occupied_until=NULL WHERE occupied_until='';
PRAGMA table_info(spots);
SELECT * FROM spots LIMIT 3;
CREATE TABLE spots_new (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
INSERT INTO spots_new (id, name, location, description, lat, lng, username)
SELECT id, name, location, description, lat, lng, username
FROM spots;
DROP TABLE spots;
ALTER TABLE spots_new RENAME TO spots;
.table
CREATE TABLE spots_new (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    location TEXT,
    description TEXT,
    lat REAL,
    lng REAL,
    username TEXT,
    occupied_by TEXT,
    occupied_until TEXT
);
INSERT INTO spots_new (id, name, location, description, lat, lng, username)
SELECT id, name, location, description, lat, lng, username
FROM spots;
DROP TABLE spots;
ALTER TABLE spots_new RENAME TO spots;
PRAGMA table_info(spots);
UPDATE spots
SET occupied_by=NULL,
    occupied_until=NULL;
SELECT id, name, occupied_by FROM spots;
SELECT id, name, occupied_by FROM spots;
INSERT INTO spots (id, name, location, description, lat, lng, username)
SELECT id, name, location, description, lat, lng, username
FROM spots_backup;
DELETE FROM spots;
DELETE FROM sqlite_sequence WHERE name='spots';
SELECT * FROM spots;
.exit
.tables
CREATE UNIQUE INDEX IF NOT EXISTS unique_favorite 
ON favorites (username, spot_id);
DELETE FROM users;
PRAGMA table_info(ratings);
.exit
.tables
DELETE FROM spots WHERE username = 'system';
DELETE FROM spots WHERE username = 'system';
DELETE FROM spots WHERE username = 'system';
.exit
DELETE FROM spots WHERE location = 'Onbekend';
DELETE FROM spots WHERE location = 'Onbekend';
.schema spots
DELETE FROM spots WHERE location = 'Onbekend';
DELETE FROM spots;
DELETE FROM sqlite_sequence WHERE name='spots';
DROP TABLE spots;
.tables
.exit
SELECT COUNT(*) FROM spots WHERE name != '🪑 Bankje';
.exit
